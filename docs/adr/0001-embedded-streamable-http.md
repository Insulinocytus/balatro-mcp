# 使用嵌入式无状态 Streamable HTTP

游戏内 MCP 服务器运行在独立启动的 Balatro 进程中，因此不适用标准输入/输出的子进程生命周期，而原始 TCP 又会要求非标准客户端适配器。服务器因此在 `127.0.0.1` 上暴露标准无状态 Streamable HTTP，并将游戏访问派发到 Balatro 主线程；首版不使用可选 SSE、Session 或身份验证，只信任本机进程，并验证 `Host` 和 `Origin` 以阻止浏览器跨来源访问。服务器不识别 Controller；并发修改仅由状态哈希仲裁，第一个有效动作成功，后续动作因状态过期而失败。
